<Window x:Class="Com.Ericmas001.Windows.MainTabWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Name}" Width="{Binding Width, Mode=TwoWay}" WindowStartupLocation="CenterScreen" Height="{Binding Height, Mode=TwoWay}" Icon="{Binding Icon}">
    <DockPanel>

        <ContentPresenter Name="Presenter" Content="{Binding Content}"/>

    </DockPanel>
</Window>